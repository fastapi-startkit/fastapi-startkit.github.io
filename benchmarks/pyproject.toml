[project]
name = "fastapi-startkit-benchmarks"
version = "0.1.0"
description = "Reproducible overhead-delta benchmark: raw FastAPI vs FastAPI Startkit"
requires-python = ">=3.12"
dependencies = [
    "fastapi-startkit[fastapi]>=0.46.0",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27",
]
